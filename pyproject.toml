[project]
name = "credit-card-fraud-detection"
version = "0.1.0"
description = "Credit Card Fraud Detection MLOps Pipeline"
requires-python = ">=3.8"
dependencies = [
    "pandas==2.3.2",
    "scikit-learn==1.2.2",
    "zenml",
    "mlflow",
    "evidently",
    "xgboost",
    "lightgbm"
]
